#include <stdio.h>

int main() {

        int score = 75;

        if (score >= 70) {
                printf("PASS!");
        } else {
                printf("FAIL!");
        }

        return 0;

}
